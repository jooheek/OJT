<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.joohee.A00.Mapper.BoardMapper">
	<select id ="getList" resultType="java.util.Map">
		SELECT *
		from project A LEFT JOIN finance B
		ON A.projectCode = B.projectCode
	
	</select>
	
	<select id="projectId" resultType="java.util.Map">
		SELECT *
		from project A, finance B
		WHERE A.projectCode = B.projectCode AND A.projectCode= #{projectCode}
	</select>


</mapper>